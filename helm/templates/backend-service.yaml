apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend
spec:
  selector:
    app: backend

  ports:
    - name: backend-port
      protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 8000

  type: {{ .Values.backend.service.type }}