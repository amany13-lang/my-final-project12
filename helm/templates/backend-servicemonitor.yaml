apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: backend

  endpoints:
    - port: backend-port
      path: /metrics
      interval: 15s